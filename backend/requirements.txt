fastapi>=0.115,<1.0
uvicorn[standard]>=0.30,<1.0
python-multipart>=0.0.9,<1.0
# Optional runtime dependency for building-footprint inference.
# Install this on the deployment/inference environment when using a trained YOLO model.
ultralytics>=8.3,<9.0
opencv-python>=4.8,<5.0
